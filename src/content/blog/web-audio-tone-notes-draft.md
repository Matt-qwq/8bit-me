---
title: '（草稿）Web Audio 起手式：从 oscillator 到音色'
date: 2025-08-10
description: 'draft fixture：用于验证 draft: true 的文章不会渲染到 /blog 列表与 [slug] 路由（生产构建）。'
tags: ['Web Audio API', 'draft']
cover: '🎛️'
draft: true
---

# Web Audio 起手式（草稿）

这是一篇 `draft: true` 的文章，用于验证 draft 过滤逻辑。

在 v1 中，draft 文章不应出现在 `/blog` 列表，也不应生成 `/blog/[slug]` 页面。
你可以对比 `astro build` 产物确认这一点。